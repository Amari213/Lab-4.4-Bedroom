package OOP.Bedroom;

public class Door {

    private String color;
    private Size size; //length and width

    private boolean isOpen;


    public Door(String color, Size size, boolean isopen) {
        this.color = color;
        this.size = size;
        this.isopen = isOpen;
    }

    public String getColor() {
        return color;
    }

    public void setColor(String color) {
        this.color = color;
    }

    public Size getSize() {
        return size;
    }

    public void setSize(Size size) {
        this.size = size;
    }

    public boolean isOpen() {
        return isOpen;
    }

    public void setOpen(boolean drawerOpen) {
        isOpen = open;
    }
    @Override
    public String toString(){
        return "Door(" + x + ", " + y + ")";
    }
}

